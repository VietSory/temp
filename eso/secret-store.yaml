apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: fake-store
  namespace: demo
spec:
  provider:
    fake:
      data:
        - key: demo/db/password
          value: "rotated-password-v2"
